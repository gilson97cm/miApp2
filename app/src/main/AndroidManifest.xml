<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deals">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity android:name=".frm_edit_products"></activity>
        <activity android:name=".frm_edit_categories" />
        <activity android:name=".frm_edit_deals" />
        <activity android:name=".frm_add_products" />
        <activity android:name=".details_category" />
        <activity android:name=".frm_add_categories" />
        <activity android:name=".details_deal" />
        <activity android:name=".frm_add_deals" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>